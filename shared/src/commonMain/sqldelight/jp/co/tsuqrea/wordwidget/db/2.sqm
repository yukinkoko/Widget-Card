-- v2 → v3: 日別×単語の表示ログ（曜日チップのタップで過去日のデイリーを見るため）
CREATE TABLE dayWordEntity (
    epochDay INTEGER NOT NULL,
    wordId TEXT NOT NULL,
    encounters INTEGER NOT NULL,
    PRIMARY KEY (epochDay, wordId)
);
